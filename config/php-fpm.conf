[global]
; 由 supervisord 管理前台运行
daemonize = no

; 主进程错误输出到容器 stderr，便于在平台日志中查看
error_log = /proc/self/fd/2

; 使用可写的 pid 路径（nobody 用户可写）
pid = /tmp/php-fpm84.pid

; 加载池配置
include = /etc/php84/php-fpm.d/*.conf
