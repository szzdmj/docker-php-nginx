[global]
; Run in foreground (supervisor manages it)
daemonize = no

; Send master errors to container stderr
error_log = /proc/self/fd/2

; Use a writable pid path under nobody
pid = /tmp/php-fpm84.pid

; Load pool configs
include = /etc/php84/php-fpm.d/*.conf
